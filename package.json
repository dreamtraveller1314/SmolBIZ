{
  "name": "smolbiz",
  "private": true,
  "version": "1.0.0",
  "description": "SmolBIZ — Manage. Empower. Grow. (static HTML/CSS/JS, Supabase, Groq)",
  "scripts": {
    "build": "node scripts/generate-config.js",
    "start": "npm run build && python3 -m http.server 8080"
  }
}
